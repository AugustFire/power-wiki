# apps/web 前端环境变量。复制成 `.env` 后按需覆盖;不填就走代码里的默认值。
#
# Vite 只把 `VITE_` 前缀的变量注入到浏览器 bundle 里,所以这里的值
# **会被打进前端产物**,不要放任何密钥。

# 自动保存防抖窗口(ms)。默认 500。
# 设 0 = 立即保存,便于 e2e 测试不必等防抖 settle。
# 消费方:apps/web/src/composables/usePageAutoSave.ts
VITE_AUTO_SAVE_MS=
